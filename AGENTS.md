# AI 应用开发工程宪法

本文件是 AI Agent 的工作约束。它只规定跨项目都值得保留的原则。

## 1. 工作原则

- 先理解需求、现有架构和边界，再修改代码；优先局部、可验证的改动。
- 沿用项目已有技术栈、目录结构和公共接口，不为追求新技术而重构。
- 复用已有组件、框架能力和工具；新增抽象必须能减少真实复杂度或重复。
- 不确定、发现边界冲突或需求存在歧义时，明确说明假设并请求确认，不擅自扩大范围。
- 代码、文档和配置保持一致；发现不一致时报告影响和修复建议。

## 2. 需求与方案决策

在新增功能、架构调整或技术选型前，至少明确：

- 目标、非目标、输入输出、成功标准和失败行为。
- 业务边界、技术边界、数据边界和安全边界。
- 数据形态、规模、延迟、可靠性、成本和运维约束。
- 至少一个可行替代方案，以及选择方案的收益、代价和已知风险。
- 验证方式：测试、固定评测集、小规模实验或可观测指标。

从最小可用方案开始。只有明确需求无法满足时，才增加框架、服务、Agent 或数据处理层。外部依赖必须定义超时、重试边界、失败状态和确定性降级路径。

## 3. 代码与模块

- 模块职责清晰，依赖方向可解释；业务逻辑不散落在入口、路由、脚本或展示层。
- 公共接口保持稳定；跨模块通过公开接口访问，不依赖内部实现路径。
- 配置、密钥、连接信息和环境差异不硬编码，使用项目既有配置入口和环境变量。
- 外部 I/O 设置合理超时；资源、连接、锁和事务有明确的释放、提交和回滚路径。
- 捕获能够处理的具体异常，保留诊断上下文；禁止裸捕获和静默吞错。
- 注释解释决策原因，不复述代码；遵循项目已有的语言和格式约定。

## 4. AI 与 Agent 应用

- 模型调用、Agent 循环、工具调用和并发必须有预算上限；耗尽后返回合法降级结果。
- 删除、退款、发布、写入外部系统等高风险或不可逆操作必须显式审批，未批准默认拒绝。
- 工具权限由运行时权限控制，不用提示词代替鉴权；上下文传递遵循最小必要原则。
- 跨模块或需持久化的模型输出使用结构化 schema 校验，不依赖正则解析自由文本。
- 记录必要的调用、状态、失败和降级信息，避免记录密钥、完整敏感输入和隐私数据。
- 多 Agent 系统显式定义消息契约、职责、调用关系、权限和可回放的关键状态。

## 5. 安全与数据

- 密钥和敏感配置只存放在受控配置或密钥管理系统中，不提交到版本库。
- 所有数据读写同时校验身份、权限、资源归属和业务规则；客户端传入 ID 不代表拥有操作权。
- 使用参数化查询或 ORM，禁止拼接用户输入形成查询、命令或模板。
- 敏感数据按项目策略脱敏、加密、最小化保存和限制日志传播。
- 重要数据的删除、修改和跨表写入遵循事务、审计、备份和保留策略。
- 采集、处理和调用外部服务必须符合授权、合同、隐私和适用法律要求。

## 6. 测试与验收

核心路径至少覆盖正常、边界和异常行为；涉及权限或外部副作用时，还要覆盖未认证、无权限、越权、重复提交、超时和降级。

验收不能只看“有结果”或 HTTP 状态码，应核对：

- 结果是否符合业务预期；
- 错误是否被正确拦截和表达；
- 数据是否持久化正确、事务是否可回滚；
- 权限和数据边界是否有效；
- 关键过程是否可观测；
- 测试、类型检查、lint、构建或固定评测是否通过。

没有实际执行的验证必须明确标记为未验证，不以推测替代证据。

## 7. 修改流程

1. 诊断：定位问题、根因、触发条件和影响范围。
2. 设计：定义修改目标、最小方案、替代方案和兼容性影响。
3. 实施：只修改相关模块，避免顺手重构；不擅自引入新依赖。
4. 验证：先运行触及范围最小的测试或检查，再根据结果扩大验证范围。
5. 交付：说明修改文件、行为变化、验证结果、未解决风险和文档影响。

禁止未经分析的猜测性修改、为了通过检查而隐藏错误、未经授权的提交或发布操作。

## 8. 项目文档与 Agent 协作

- Agent 进入项目后先读取本文件，再读取项目架构、README、变更记录和相关 skill。
- 文档优先级为：安全与边界约束、本文件、项目架构文档、README、其他说明；发现冲突必须报告。
- 项目特有约束应写入项目文档或专用 skill，不要把领域规则无条件扩展为全局规则。
- skill 只在任务相关时按需加载；通用常识不重复写入 skill。
- Agent 不自动执行 commit、push、merge 或发布，除非用户明确授权。
